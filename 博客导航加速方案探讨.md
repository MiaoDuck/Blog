# 博客导航加速方案探讨

> 记录于 2026-06-29。围绕"点击博客条目触发整页刷新"问题的现状分析、方案对比与最终落地。

## 一、问题现状

Hextra 是**传统多页静态站点主题**，无任何客户端导航机制。对 `themes/hextra/` 全量排查结论：

- 无 pjax / Turbo / InstantClick / swup / view-transition / `history.pushState`
- 无任何前端框架（React/Vue/Alpine 等）
- 无 instant.page 或链接 prefetch
- 唯一的 `fetch()` 用于搜索索引和 Markdown 复制，与导航无关

博客列表条目均为标准 `<a href="{{ .RelPermalink }}">`，浏览器原生整页加载：

```21:21:themes/hextra/layouts/blog/list.html
            <h3><a style="color: inherit; text-decoration: none;" class="hx:block hx:font-semibold hx:mt-8 hx:text-2xl " href="{{ .RelPermalink }}">{{ .Title }}</a></h3>
```

```28:35:themes/hextra/layouts/blog/list.html
            <p class="hx:opacity-80 hx:mt-1 hx:leading-7">
              <a
                class="hx:text-[color:hsl(var(--primary-hue),100%,50%)] hx:underline hx:underline-offset-2 hx:decoration-from-font"
                href="{{ .RelPermalink }}"
                aria-label="{{ printf "%s %s" $readMore .Title }}"
              >
                {{- $readMore -}}
              </a>
            </p>
```

整页刷新代价：重新下载 HTML → 重新解析 CSS/JS → 重跑全部脚本（含 Mermaid 的 `mermaid.run()`、主题 JS 重绑、FlexSearch 索引重建）→ 字体/样式可能闪烁 → 丢失滚动位置。

## 二、Hextra 的脚本架构（关键约束）

主题有 **16 个核心 JS 模块**，全部在 `DOMContentLoaded` 一次性 IIFE 绑定：

| 模块 | 用途 |
|------|------|
| `core/theme.js` | 主题切换 |
| `core/toc-scroll.js` | TOC 滚动高亮 |
| `core/code-copy.js` | 代码块复制 |
| `core/menu.js` | 移动端汉堡菜单 |
| `core/sidebar.js` | 侧边栏折叠 |
| `core/tabs.js` | 标签页切换 |
| `core/lang.js` | 语言切换 |
| `core/nav-menu.js` | 导航栏下拉 |
| `core/page-context-menu.js` | 页面上下文菜单 |
| `core/back-to-top.js` | 返回顶部 |
| `core/banner.js` | 顶部横幅 |
| `core/alert.js` | GitHub alert 折叠 |
| `core/favicon.js` | favicon 切换 |
| `core/filetree.js` | 文件树折叠 |
| `core/task-list.js` | 任务列表增强 |
| `flexsearch.js` | 搜索引擎 |

**这些脚本都是闭包 IIFE，无法从外部重新调用。** 一旦 DOM 被替换，绑定全部失效。这是后续所有方案选型的核心约束。

## 三、方案对比

| 方案 | 效果 | 复杂度 | 风险 | 建议 |
|------|------|--------|------|------|
| **A. instant.page** | 悬停预获取，点击命中缓存秒开 | 极低 | 极低 | ✅ 主推 |
| **B. Speculation Rules** | 后台预渲染，点击瞬间显示 | 低 | 低 | ✅ 现代浏览器进阶 |
| **C. SPA 改造（swup/pjax）** | 真正无刷新导航 | 高 | 高 | ❌ 不推荐 |
| **D. Turbo Drive** | pjax，只换 body | 中高 | 高 | ❌ 不推荐 |
| **E. MPA View Transitions** | 整页刷新但有过渡动画 | 低 | 低 | ◻ 可选补充 |

### 为什么不推荐 SPA / Turbo Drive

核心原因：**Hextra 的 16 个脚本是一次性 IIFE 绑定，DOM 替换后无法重新初始化。**

- SPA/pjax/Turbo 换 `<body>` 后，旧 DOM 元素消失、事件绑定失效，新元素无人管理；
- 必须手动监听 `turbo:load` / 自定义事件重跑所有初始化，但脚本闭包不可外部调用；
- 等价于在主题之上再维护一套"主题引导层"，主题升级极易破坏；
- 换来的体验提升相对预获取方案只有几十毫秒，用户基本无感。

**Turbo Drive 补充说明**：它是 Hotwire 框架的 pjax 组件，"持久化脚本"特性与 Hextra 的"一次性绑定"架构冲突。适合自控前后端的项目（Rails+Hotwire），不适合第三方固定主题。

### 为什么预获取方案接近 SPA 体验

`instant.page` + Speculation Rules 组合：

- 用户**悬停**链接时，浏览器已在后台 fetch 完目标 HTML，甚至（Chromium）预渲染完毕；
- 用户**点击**时从内存缓存直接显示，体感与 SPA 几乎无异；
- 滚动位置、锚点、前进后退由浏览器原生处理，零 bug 风险；
- 真正差距只剩"脚本重跑时间"（< 50ms），用户无感。

## 四、最终落地方案

在 `layouts/_partials/custom/head-end.html`（Hextra 官方自定义钩子）追加：

```html
{{- /* 导航加速：instant.page 悬停预获取 + Speculation Rules 后台预渲染 */ -}}
<script type="module" src="https://cdn.jsdelivr.net/npm/instant.page@5.2.0/instant.page.min.js"></script>
<script type="speculationrules">
{
  "prerender": [
    { "where": { "href_matches": "/blog/**" }, "eagerness": "moderate" }
  ]
}
</script>
```

### 两段代码分工

| 代码 | 机制 | 兼容性 |
|------|------|--------|
| `instant.page` | 悬停/按下时 `fetch` 预获取 HTML 进缓存 | 所有现代浏览器 |
| `speculationrules` | Chromium 后台预渲染整页（含 JS 执行） | Chrome 121+，其他浏览器忽略 |

`eagerness: "moderate"` 表示悬停时才预渲染，资源消耗可控。非 Chromium 浏览器自动降级为 instant.page 预获取。

### 可选补充：MPA View Transitions

在 `assets/css/custom.css`（不存在则新建）加一行即可给整页刷新加过渡动画：

```css
@view-transition { navigation: auto; }
```

Chrome 111+ 生效，可与上述方案叠加。

## 五、已完成的提交

- 提交 `0683ab6`：修改 `layouts/_partials/custom/head-end.html`
- 已推送到 `origin`（CNB `cnb.cool/miaoduck/Boke`）和 `github`（GitHub `MiaoDuck/Blog`）的 `main` 分支
- Cloudflare 自动构建发布

## 六、验证方式

部署完成后在 https://blog.mkka27834.workers.dev/ 打开博客列表：

1. **悬停**任意博客条目标题，DevTools Network 面板应看到对该文章页的 `fetch` 预获取请求（instant.page 生效）；
2. **点击**时页面应几乎秒开（命中缓存）；
3. Chrome/Edge 中悬停还可能触发后台预渲染（Speculation Rules），点击瞬间切换。

## 七、关于双仓库工作流

项目采用 CNB（主开发）+ GitHub（部署镜像）双仓库模式。一个工作区同时推两个仓库的推荐配置：

### SSH key 方式（推荐）

在本地开发机器一次性配置：

```bash
# 1. 生成 key
ssh-keygen -t ed25519 -C "your_email@example.com" -f ~/.ssh/id_ed25519 -N ""

# 2. 公钥添加到 GitHub 和 CNB
cat ~/.ssh/id_ed25519.pub

# 3. 验证
ssh -T git@github.com
ssh -T git@cnb.cool

# 4. remote 改 SSH
git remote set-url origin git@cnb.cool:miaoduck/Boke.git

# 5. origin 配多 push URL（删 github remote，改用 origin 双推）
git remote remove github
git remote set-url --add --push origin git@cnb.cool:miaoduck/Boke.git
git remote set-url --add --push origin git@github.com:MiaoDuck/Blog.git

# 6. 之后一条命令双推
git push origin main
```

### 注意事项

- `git clone` 只自动建 `origin`，`github` remote 需手动添加；
- SSH key 配置存在本地 `.git/config`，换机器/重新 clone 需重配；
- fetch 只拉 CNB（主仓库），push 同时推两仓。
